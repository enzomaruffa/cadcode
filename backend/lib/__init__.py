"""Shared CAD library: design tokens + reusable parametric parts (plan §5).

Importable from the sandboxed user script (``from lib.design import *``,
``from lib.parts import m3_boss``). The agent reads these so "add a boss" yields
*your* wall thickness and fit, not generic defaults.
"""
